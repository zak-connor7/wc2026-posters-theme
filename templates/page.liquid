<div class="container section-lg" style="padding-top: calc(var(--spacing-xl) + 80px); max-width: 800px;">
  <p class="heading-sm" style="color: var(--color-text-muted); margin-bottom: 1.5rem;">{{ page.title }}</p>
  <div class="body-lg" style="color: var(--color-text-muted); line-height: 1.9;">
    {{ page.content }}
  </div>
</div>
